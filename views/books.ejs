<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Bookstore App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Bookstore App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <% if (user.role === "admin") { %>
                <li class="nav-item">
                  <a class="nav-link" href="/admin">Admin Dashboard</a>
                </li> 
                <li class="nav-item">
                  <a class="nav-link" href="/addBook">Add new book</a>
                </li> 
              <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <p>Welcome, <%= user.username %> (<%= user.role %>)</p>
        <br>
        <div class="text-center"><h2>Book Details</h2></div>
        <br>
        <% if (book) { %> <!-- Check if book data exists -->
            <div>
                <p><b>Book Name:</b><br> <%= book.bookName %></p>
                <p><b>Book Image:</b><br> <img src = "../images/<%= book.image %>" width="20%"></p>
                <p><b>Book Quantity:</b><br> <%= book.quantity %></p>
                <p><b>Price:</b><br> $<%= book.price.toFixed(2) %></p> 
            </div>
        <% } else { %>
            <p>No book found.</p>
        <% } %>
        <% if (user.role === "admin") { %>
          <a href ="/admin">Back</a>
        <% } else { %>
          <a href ="/shopping">Back</a>
        <% } %>
    </div>
    
</body>
</html>
